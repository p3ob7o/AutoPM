{
  "name": "autopm",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "autopm": "src/cli/autopm.ts"
  },
  "scripts": {
    "render": "bun run src/cli/autopm.ts render",
    "deploy": "bun run src/cli/autopm.ts deploy",
    "start": "bun run src/cli/autopm.ts run",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "gray-matter": "^4.0.3",
    "hono": "^4.12.0",
    "yaml": "^2.9.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
